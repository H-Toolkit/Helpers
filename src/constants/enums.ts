export enum RecognizeFiles {
	extension = 'extension',
	implicate = 'implicate',
}

export enum PathType {
	relative = 'relative',
	absolute = 'absolute',
}

export enum OperationSystemTypes {
	Windows_NT = 'Windows',
	Darwin = 'macOS',
	Linux = 'Linux',
}
